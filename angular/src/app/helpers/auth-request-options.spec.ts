import { AuthRequestOptions } from './auth-request-options';

describe('AuthRequestOptions', () => {
  it('should create an instance', () => {
    expect(new AuthRequestOptions()).toBeTruthy();
  });
});
